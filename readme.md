# Call Center Cloud CM Behavior driven development

Example of BDD for a Contact Center Cloud solution.

To get started:

- Create a virtualenv for this project
- Install requirements using `pip install -r requirements.txt`

When you've created the first test, you'll also need to create a correct runtime configuration in PyCharm.
